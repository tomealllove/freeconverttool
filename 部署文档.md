# 在线文件工具站 · 部署文档

> 纯前端（无任何后端 / 服务器逻辑）的静态网站。所有文件处理均在浏览器本地完成，不依赖任何自有服务，可直接放到任意静态托管平台。

---

## 1. 项目简介

一个 15 页面的纯前端在线文件工具站，覆盖图片、PDF、音视频、电子书、汉字、证件照与二维码等常用转换能力。

| 分类 | 页面 |
| --- | --- |
| 图片工具 | 图片格式转换、图片转 PDF、图片转 GIF、图片压缩 |
| PDF 工具 | PDF 转 Word、PDF 合并拆分、PDF 压缩、PDF 转图片 |
| 转换工具 | 音频格式转换、视频格式互转、电子书格式互转 |
| 汉字转换 | 繁简汉字转换、汉字大小写（含金额大写） |
| 证件照与码 | 证件照生成、二维码生成 |

- 处理全程在**浏览器本地**完成，文件**不会**上传到任何服务器（除第三方库按需从 CDN 加载外）。
- 统一交互流程：上传/拖拽 → 参数设置 → 执行 → 预览 / 下载 / 复制。
- 左侧固定多级折叠导航，高亮当前页。

---

## 2. 环境要求

| 项目 | 要求 |
| --- | --- |
| 浏览器 | 现代 Chromium 内核（Chrome / Edge ≥ 90）、Firefox ≥ 90、Safari ≥ 15 |
| 后端 | **不需要**（无服务端代码，无构建步骤） |
| 运行环境 | 仅需一个能托管静态文件的 Web 服务器或直接以 `file://` 打开 |
| 网络 | 首次使用某功能需联网（加载对应第三方库 CDN）；其余逻辑可离线 |

---

## 3. 目录结构

```
file-tools/
├── index.html                 # 站点首页 / 总览入口
├── README.md                  # 功能与已知边界说明
├── 部署文档.md                # 本文档
├── <工具页>.html              # 15 个独立页面（见上表）
└── assets/
    ├── css/style.css          # 全站样式
    └── js/
        ├── common.js          # 公共层 FT（导航/Toast/拖拽/下载/ZIP/PDF 封装等）
        └── pages/
            └── <同名>.js       # 每个页面一个脚本（IIFE，首行 FT.mountShell('key')）
```

> 页面与脚本一一对应：`img-convert.html` ↔ `assets/js/pages/img-convert.js`，共 15 对。

---

## 4. 本地预览（开发与自测）

有三种方式，推荐方式 A。

### A. 本地静态服务器（推荐）
```bash
cd file-tools
python -m http.server 8000
# 浏览器访问 http://localhost:8000
```
优点：无 `file://` 限制，GIF 导出、Worker 类功能（gif.js）全部正常。

### B. 直接用浏览器打开
双击 `index.html` 或任意 `<工具页>.html`（`file://` 协议）。
- 多数功能可用；
- **限制**：`图片转 GIF` 的 gif.js 依赖 Web Worker，在部分浏览器 `file://` 下会被拦截，建议用方式 A。

### C. Node 静态服务（可选）
```bash
npx serve file-tools      # 或 npx http-server file-tools
```

---

## 5. 部署到生产

由于是纯静态站点，**直接把整个 `file-tools/` 目录上传即可**，无需构建、无需安装依赖。

### 5.1 任意静态托管平台

| 平台 | 做法 |
| --- | --- |
| **GitHub Pages** | 把 `file-tools/` 内容推到仓库，Settings → Pages 选择分支与 `/` 根目录 |
| **Vercel / Netlify** | 拖拽 `file-tools/` 目录，或连接 Git 仓库；构建命令留空，输出目录设为 `file-tools` |
| **对象存储 + CDN**（OSS / COS / S3） | 将目录整体上传为静态网站，开启「静态托管」 |
| **Nginx / Apache** | 把目录放到站点根（如 `/usr/share/nginx/html/file-tools`），无需额外配置 |
| **内网 / 局域网服务器** | 同上，拷贝目录即可 |

### 5.2 Nginx 最简配置示例
```nginx
server {
    listen 80;
    server_name tools.example.com;
    root /var/www/file-tools;
    index index.html;
    # 纯静态，无需 rewrite / 后端
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### 5.3 部署注意事项
- **保留目录结构**：`assets/` 必须与原 `file-tools/` 同级，否则样式与脚本 404。
- **无需 HTTPS 强制**，但建议开启 HTTPS（部分浏览器在 HTTP 下对 Web Worker / 某些 API 有限制）。
- **无需配置 CORS**：第三方库从公共 CDN 加载，由 CDN 提供跨域头。
- **缓存**：`assets/` 下文件内容固定，可设长缓存（`Cache-Control: max-age=86400`）。

---

## 6. 第三方依赖（CDN）

以下功能首次使用时会从公共 CDN 按需加载，需联网：

| 库 | 用途 | CDN 来源 |
| --- | --- | --- |
| pdf.js | PDF 解析 / 渲染 | cdn.jsdelivr.net |
| pdf-lib | PDF 结构重组 / 压缩 | cdn.jsdelivr.net |
| gif.js | GIF 编码 | cdn.jsdelivr.net |
| lamejs | MP3 编码 | cdn.jsdelivr.net |
| @ffmpeg/ffmpeg | 高级音视频编码（AAC/FLAC 等） | unpkg.com |
| JSZip | ZIP 打包 | cdn.jsdelivr.net |
| qrcode-generator | 二维码生成 | cdn.jsdelivr.net |
| opencc-js | 繁简转换 | cdn.jsdelivr.net |
| @mediapipe/tasks-vision | 证件照 AI 抠图（增强） | cdn.jsdelivr.net |

- **白名单**：仅 `cdn.jsdelivr.net` / `cdnjs.cloudflare.com` / `unpkg.com`。
- **离线兜底**：ZIP 打包内置纯 JS 的 store 算法兜底；其余功能无网络时对应按钮会提示「依赖未加载」。
- **内网 / 完全离线部署**：如需彻底离线，可把上述库下载到 `assets/vendor/` 并修改 `common.js` 中的 CDN 地址为本地路径（属于定制改造，不在本次交付范围）。

---

## 7. 已知边界与能力限制

纯前端方案的客观限制（各页面内已有明确提示）：

- **PDF 转 Word**：导出为「文字 + 基础排版」的 `.docx`，**不做 OCR**；扫描件图片型 PDF 转出的文字可能为空。
- **音频**：浏览器原生无 AAC / FLAC 编码器，这两个格式走 ffmpeg.wasm 高级模式（首次约 30MB 引擎下载，纯 CPU 软编码、较慢）。
- **视频**：ffmpeg.wasm 无硬件加速，**仅适合短视频**；长视频请用专业工具。
- **电子书**：EPUB / TXT 互转完整支持；MOBI / AZW3 等私有格式**拒绝处理**（前端无法解析）。
- **证件照**：默认用纯色容差抠图（边缘估背景色 + 双阈值羽化）；AI 抠图（MediaPipe）失败自动回退到纯色方案。
- **CAJ / HEIC**：CAJ 为私有格式，前端无法解析，已用占位说明；HEIC 依赖浏览器原生解码，部分浏览器不支持时提示升级。

---

## 8. 常见问题排查

| 现象 | 可能原因 | 处理 |
| --- | --- | --- |
| 打开后样式错乱 / 脚本不执行 | 目录结构被破坏，`assets/` 丢失 | 确保 `assets/` 与 HTML 同级 |
| GIF 导出无反应 / 一直转圈 | `file://` 下 Worker 被拦截 | 改用本地服务器（方式 A） |
| 某功能提示「依赖未加载」 | 网络无法访问 CDN | 检查网络 / 确认 CDN 白名单可达 |
| PDF 转 Word 文字为空 | 源 PDF 为图片型扫描件，无文字层 | 该能力不做 OCR，属已知限制 |
| 视频转换卡住 | 文件过大 / 浏览器内存限制 | 换用短视频，或专业工具 |
| 音频高级模式很慢 | ffmpeg.wasm 纯软编码 | 属正常，耐心等待或换原生模式 |
| 首页外链只有默认 3 个 | 以 `file://` 打开导致 XML 读取被拦截；或 `links.xml` 语法错误 | 改用本地服务器访问；检查 XML 注释里是否含连续的 `--` |

---

## 9. 隐私与安全

- 所有文件处理在用户**本地浏览器**完成，不上传任何服务器。
- 本站点**不收集**任何用户文件、操作日志或个人数据。
- 第三方库仅用于算法计算，不涉及数据外传。
- 部署方若接入统计 / 日志服务，请自行评估合规要求。

---

## 10. 首页外链配置（links.xml）

首页底部的「常用外链」区由 `assets/data/links.xml` 驱动，**最多 12 个位置**。

```xml
<links max="12">
  <item>
    <name>Google</name>
    <url>https://www.google.com</url>
  </item>
  <item>
    <name>Baidu</name>
    <url>https://www.baidu.com</url>
  </item>
  <!-- 其余为预留位：url 留空则不显示，填上即自动生效 -->
  <item>
    <name></name>
    <url></url>
  </item>
</links>
```

规则与说明：

- `name` 留空则自动显示该链接的域名；`url` 为空的条目视为**预留位**，不会显示。
- `url` 可省略协议，会自动补全为 `https://`（例如填 `example.com`）。
- 所有外链自动带 `target="_blank"` 与 `rel="noopener noreferrer nofollow"`（安全与 SEO 友好）。
- **注意**：XML 注释中**不能出现连续的短横线 `--`**，否则文档不合法、解析会失败。
- **回退机制**：若 XML 加载或解析失败（例如直接以 `file://` 双击打开首页，浏览器会拦截本地文件读取），将回退显示页面内置的 3 个默认外链（Google / Baidu / Bing）。
- 因此请**通过本地服务器或线上访问**来预览 XML 配置效果；修改后用 `Ctrl + F5` 强制刷新。

实现文件：`assets/js/pages/home.js`（读取 XML 并渲染外链，同时负责挂载侧边栏）。

---

## 11. 交付清单

- `file-tools/` 目录：39 个文件（16 个 HTML + 16 个页面脚本 + style.css + common.js + robots.txt + sitemap.xml + links.xml + og-cover.png + README.md + 部署文档.md）
- `file-tools.zip`：完整压缩包（已排除测试目录，可直接解压部署）
- `overview.md`：交付说明
- 本文档：`部署文档.md`
